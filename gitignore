# 실제 회사 데이터(공구현황판, 결산자료 등)는 GitHub에 올리지 않습니다
data/

# archive_data/ 는 예외입니다 — 25년처럼 더 이상 바뀌지 않는 과거 자료를 코드와 함께 보관하는 용도로,
# 일부러 gitignore 하지 않고 커밋합니다.

# Python 캐시
__pycache__/
*.pyc

# OS/에디터 관련
.DS_Store
.vscode/
